# 酒店空调管理系统 - 前端项目说明文档

## 📦 项目概述

这是一个基于 **Vue 3** 的酒店空调管理系统前端项目，支持多房间空调控制、实时状态监控和费用计算。

**技术栈**: Vue 3 + Vite + Pinia + Vue Router + Tailwind CSS + Axios + Socket.IO

---

## 📂 项目结构树状图

```
hotel-ac-frontend/
│
├── 📄 package.json                     # 项目依赖和脚本配置
├── 📄 .env                             # 环境变量配置（API 地址、WebSocket 地址）
├── 📄 vite.config.js                   # Vite 构建工具配置
├── 📄 tailwind.config.js               # Tailwind CSS 样式配置
├── 📄 postcss.config.js                # PostCSS 配置
│
├── 📁 public/                          # 静态资源文件夹
│   └── ...
│
└── 📁 src/                             # 源代码目录 ⭐
    │
    ├── 📄 main.js                      # ⭐⭐⭐ 应用入口文件
    │   ├── 初始化 Vue 应用
    │   ├── 配置 Pinia 状态管理
    │   ├── 配置 Vue Router 路由
    │   └── 挂载根组件
    │
    ├── 📄 App.vue                      # ⭐ 根组件
    │   └── 提供深色背景布局和路由出口
    │
    ├── 📄 style.css                    # ⭐ 全局样式文件
    │   └── 导入 Tailwind CSS 基础样式
    │
    ├── 📁 components/                  # 组件文件夹
    │   │
    │   └── 📄 roomACPanel.vue          # ⭐⭐⭐ 空调控制面板组件（核心 UI）
    │       ├── 功能1: 显示房间号和空调状态
    │       ├── 功能2: 开关机控制按钮
    │       ├── 功能3: 实时显示当前温度和目标温度
    │       ├── 功能4: 温度调节按钮（+0.5/-0.5°C）
    │       ├── 功能5: 风速切换按钮（低/中/高）
    │       ├── 功能6: 显示服务状态（服务中/等待中/已关闭）
    │       ├── 功能7: 显示累计费用和费率
    │       ├── 功能8: WebSocket 连接状态提示
    │       ├── 功能9: 错误消息显示
    │       └── 功能10: 加载状态（防止重复点击）
    │
    ├── 📁 stores/                      # Pinia 状态管理文件夹
    │   │
    │   └── 📄 roomStore.js             # ⭐⭐⭐ 房间状态管理（核心业务逻辑）
    │       │
    │       ├── 【常量配置】
    │       │   ├── TEMP_LIMITS: 温度限制（制冷 18-25°C，制热 25-30°C）
    │       │   ├── FAN_SPEED_CONFIG: 风速配置（低/中/高风 + 费率）
    │       │   ├── VALID_FAN_SPEEDS: 有效风速列表
    │       │   ├── VALID_MODES: 有效模式列表
    │       │   └── SERVICE_STATUS: 服务状态常量
    │       │
    │       ├── 【状态数据】
    │       │   ├── roomId: 房间号
    │       │   ├── isPowerOn: 是否开机
    │       │   ├── currentTemp: 当前温度
    │       │   ├── targetTemp: 目标温度
    │       │   ├── fanSpeed: 风速（Low/Mid/High）
    │       │   ├── mode: 模式（Cooling/Heating）
    │       │   ├── totalFee: 累计费用
    │       │   ├── feeRate: 费率（元/度）
    │       │   ├── status: 服务状态（idle/serving/waiting）
    │       │   ├── waitTime: 等待时间（秒）
    │       │   └── isConnected: WebSocket 连接状态
    │       │
    │       ├── 【WebSocket 通信】
    │       │   ├── connectWebSocket(): 连接 WebSocket 服务器
    │       │   ├── disconnectWebSocket(): 断开连接
    │       │   ├── 监听事件: temperature-update（温度更新）
    │       │   ├── 监听事件: fee-update（费用更新）
    │       │   ├── 监听事件: status-change（状态变化）
    │       │   ├── 监听事件: service-started（服务开始）
    │       │   └── 监听事件: service-stopped（服务停止）
    │       │
    │       └── 【API 方法】
    │           ├── togglePower(): 开关机
    │           ├── setTargetTemp(temp): 设置目标温度
    │           ├── setFanSpeed(speed): 设置风速
    │           ├── queryCurrentFee(): 查询费用
    │           ├── initRoomData(): 初始化房间数据
    │           ├── setRoomId(id): 设置房间号
    │           └── resetState(): 重置状态
    │
    ├── 📁 router/                      # 路由配置文件夹
    │   │
    │   └── 📄 index.js                 # ⭐⭐ Vue Router 路由配置
    │       ├── 路由1: / → 重定向到 /room/301
    │       ├── 路由2: /room/:roomId → 房间控制面板（动态参数）
    │       ├── 路由3: 404 → 错误页面
    │       └── 路由守卫: 验证房间号格式（必须是数字）
    │
    ├── 📁 views/                       # 页面级组件文件夹
    │   │
    │   └── 📄 NotFound.vue             # ⭐ 404 错误页面
    │       └── 显示 "页面未找到" 并提供返回首页按钮
    │
    └── 📁 assets/                      # 静态资源（图片、字体等）
        └── ...
```

---

## 🔥 核心文件详解

### 1️⃣ **roomStore.js** - 状态管理核心（最重要！）

**位置**: `src/stores/roomStore.js`

**功能**:
- 管理所有房间状态数据
- 处理与后端的所有 HTTP 请求
- 处理 WebSocket 实时通信
- 提供业务常量配置

**导出的常量**:
```javascript
TEMP_LIMITS        // 温度范围配置
FAN_SPEED_CONFIG   // 风速配置（包含标签和费率）
VALID_FAN_SPEEDS   // 有效风速列表
VALID_MODES        // 有效模式列表
SERVICE_STATUS     // 服务状态枚举
```

**主要方法**:
| 方法 | 说明 | 调用的 API |
|------|------|-----------|
| `togglePower()` | 开关机 | POST /ac/request-service 或 /ac/stop-service |
| `setTargetTemp(temp)` | 设置温度 | POST /ac/set-temperature |
| `setFanSpeed(speed)` | 设置风速 | POST /ac/set-fan-speed |
| `initRoomData()` | 初始化数据 | GET /ac/room-status/:roomId |
| `queryCurrentFee()` | 查询费用 | GET /ac/query-fee/:roomId |

---

### 2️⃣ **roomACPanel.vue** - 用户界面组件（最重要！）

**位置**: `src/components/roomACPanel.vue`

**功能**:
- 显示空调控制面板 UI
- 处理用户交互（点击按钮、调节温度等）
- 显示实时数据（温度、费用、状态）
- 显示错误和加载状态

**UI 区块**:
1. **顶部信息栏**: 房间号 + 状态指示器 + 开关机按钮
2. **中央仪表盘**: 当前温度大字显示 + 目标温度 + 模式
3. **控制区**: 温度调节按钮 + 风速选择按钮
4. **状态提示**: WebSocket 连接状态 + 等待队列提示
5. **底部信息**: 当前费用 + 费率 + 操作提示

---

### 3️⃣ **router/index.js** - 路由配置

**位置**: `src/router/index.js`

**路由表**:
| 路径 | 组件 | 说明 |
|------|------|------|
| `/` | 重定向 | 自动跳转到 `/room/301` |
| `/room/:roomId` | roomACPanel.vue | 显示指定房间的控制面板 |
| `/*` | NotFound.vue | 404 错误页面 |

**特性**:
- 动态路由参数 `:roomId` 支持任意房间号
- 路由守卫验证房间号格式（必须是纯数字）
- 自动设置页面标题

---

### 4️⃣ **main.js** - 应用入口

**位置**: `src/main.js`

**初始化顺序**:
1. 创建 Vue 应用实例
2. 安装 Pinia（状态管理）
3. 安装 Vue Router（路由）
4. 挂载应用到 `#app`

---

### 5️⃣ **.env** - 环境变量配置

**位置**: `.env`

**配置项**:
```env
VITE_API_BASE_URL=http://localhost:3000/api  # 后端 API 地址
VITE_WS_URL=http://localhost:3000            # WebSocket 服务器地址
VITE_APP_TITLE=酒店空调管理系统              # 应用标题
```

⚠️ **重要**: 修改后需要重启开发服务器才能生效！

---

## 🔌 后端需要提供的接口（API）

前端会调用以下 HTTP 接口：

### 1. **开机接口**
```
POST /api/ac/request-service
Content-Type: application/json

请求体:
{
  "roomId": "301",
  "currentTemp": 28.0
}

响应:
{
  "success": true,
  "mode": "Cooling",
  "targetTemp": 25.0,
  "fanSpeed": "Mid",
  "feeRate": 1.0,
  "status": "serving" | "waiting",
  "waitTime": 0  // 如果 status 是 waiting，这里是等待时间（秒）
}
```

---

### 2. **关机接口**
```
POST /api/ac/stop-service
Content-Type: application/json

请求体:
{
  "roomId": "301"
}

响应:
{
  "success": true,
  "message": "关机成功"
}
```

---

### 3. **设置温度接口**
```
POST /api/ac/set-temperature
Content-Type: application/json

请求体:
{
  "roomId": "301",
  "targetTemp": 24.5
}

响应:
{
  "success": true,
  "message": "温度设置成功"
}
```

---

### 4. **设置风速接口**
```
POST /api/ac/set-fan-speed
Content-Type: application/json

请求体:
{
  "roomId": "301",
  "fanSpeed": "High"  // "Low" | "Mid" | "High"
}

响应:
{
  "success": true,
  "message": "风速设置成功",
  "statusChanged": false,  // 是否触发了优先级调度
  "newStatus": "serving",  // 如果状态改变了，这里是新状态
  "waitTime": 0            // 如果被挂起，这里是等待时间
}
```

---

### 5. **查询房间状态接口**
```
GET /api/ac/room-status/301

响应:
{
  "success": true,
  "isPowerOn": false,
  "currentTemp": 28.0,
  "targetTemp": 25.0,
  "fanSpeed": "Mid",
  "mode": "Cooling",
  "totalFee": 0.0,
  "feeRate": 1.0,
  "status": "idle",
  "waitTime": 0
}
```

---

### 6. **查询费用接口**（可选）
```
GET /api/ac/query-fee/301

响应:
{
  "success": true,
  "totalFee": 12.50
}
```

---

## 📡 后端需要提供的 WebSocket 事件

前端会通过 Socket.IO 连接到 WebSocket 服务器，需要后端主动推送以下事件：

### 客户端发送的事件

#### 1. **加入房间频道**
```javascript
// 客户端连接成功后立即发送
socket.emit('join-room', { roomId: '301' });
```

#### 2. **离开房间频道**
```javascript
// 客户端断开前发送
socket.emit('leave-room', { roomId: '301' });
```

---

### 服务端推送的事件

#### 1. **温度更新**
```javascript
socket.emit('temperature-update', {
  roomId: '301',
  currentTemp: 27.5
});
```

#### 2. **费用更新**
```javascript
socket.emit('fee-update', {
  roomId: '301',
  totalFee: 12.50
});
```

#### 3. **状态变化**
```javascript
socket.emit('status-change', {
  roomId: '301',
  status: 'waiting',  // 'idle' | 'serving' | 'waiting'
  waitTime: 30        // 可选，等待时间（秒）
});
```

#### 4. **服务开始**
```javascript
socket.emit('service-started', {
  roomId: '301'
});
```

#### 5. **服务停止**
```javascript
socket.emit('service-stopped', {
  roomId: '301'
});
```

---

## 🚀 如何启动项目

### 前置条件
- 已安装 Node.js (v16+)
- 后端服务器运行在 `http://localhost:3000`

### 启动步骤

1️⃣ **解压项目**
```bash
# 解压 hotel-ac-frontend.zip 到任意目录
```

2️⃣ **进入项目目录**
```bash
cd hotel-ac-frontend
```

3️⃣ **安装依赖**（首次运行）
```bash
npm install
```

4️⃣ **启动开发服务器**
```bash
npm run dev
```

5️⃣ **访问项目**
```
默认地址: http://localhost:5173/
房间 301: http://localhost:5173/room/301
房间 302: http://localhost:5173/room/302
```

---

## 🔧 项目配置

### 修改后端地址

编辑 `.env` 文件：
```env
VITE_API_BASE_URL=http://你的后端地址:端口/api
VITE_WS_URL=http://你的后端地址:端口
```

⚠️ 修改后需要重启前端服务器！

---

## 📦 项目依赖

```json
{
  "dependencies": {
    "vue": "^3.5.13",              // Vue 3 核心
    "pinia": "^2.2.8",             // 状态管理
    "vue-router": "^4.5.0",        // 路由
    "axios": "^1.7.9",             // HTTP 请求
    "socket.io-client": "^4.8.1"   // WebSocket 客户端
  },
  "devDependencies": {
    "vite": "^6.0.5",              // 构建工具
    "tailwindcss": "^3.4.17",      // 样式框架
    "autoprefixer": "^10.4.20",    // CSS 后处理
    "postcss": "^8.4.49"           // CSS 转换
  }
}
```

---

## 🎯 业务规则

### 温度限制
- **制冷模式**: 18°C - 25°C
- **制热模式**: 25°C - 30°C
- **调节步长**: 0.5°C

### 风速选项
| 风速 | 英文名 | 费率（元/分钟）|
|------|--------|---------------|
| 低风 | Low    | 0.02          |
| 中风 | Mid    | 0.03          |
| 高风 | High   | 0.05          |

### 服务状态
- **idle**: 空闲（未开机）
- **serving**: 服务中（正在工作）
- **waiting**: 等待中（在队列中）

---

## 📞 联系方式

如有问题，请联系前端开发同学。

---

**祝开发顺利！** 🎉
